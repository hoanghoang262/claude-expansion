---
description: Force task-breakdown on current approved spec
---

Run **Phase: Task Breakdown** from the orchestrator now.

Read `.workflow/STATE.md` for active-spec slug, then decompose `.workflow/specs/<slug>/approved.md` into tasks.
Announce: `[workflow:task-breakdown] Starting (user-initiated)`
