---
name: spec
description: Spec operations — forming new specs and amending locked ones.
---

# Spec

Two operations. Load the relevant guide when needed:

| Operation | When | Guide |
|-----------|------|-------|
| Form a new spec | Intent is clear, no approved spec yet | `./formation.md` |
| Amend a locked spec | approved.md exists and must change | `./amendment.md` |

Load only the guide you need — don't load both.
